services:
  - type: web
    name: cryptoai-backend
    runtime: python3
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
    plan: free
    rootDir: .
    envVars:
      - key: MONGO_URL
        value: mongodb+srv://cryptouser:Dellasorte%401959@cluster0.qpnh8lc.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - key: CORS_ORIGINS  
        value: https://cryptoaidigest.com,https://www.cryptoaidigest.com
